import Model from "./Model/Model.js";
const model = new Model();

/**
 * @Description
 * Vous devez faire une petite application qui permet de récupérer un certain nombre de post(s).
 **/

// let getPost = model.getPost();
// console.log(getPost);
// model.getPost();

/**
 * @Description
 * utilisation de de création du button
 **/

model.calculButton();
model.showPost();
// console.log(await model.getNumberPost(2));
// model.getNumberPost(2);
// console.log(calculButton);
